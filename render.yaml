services:
  - type: web
    name: wedding-app-backend
    runtime: node
    buildCommand: cd backend && npm install --legacy-peer-deps
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 3000
      - key: IMGBB_API_KEY
        value: a85bf9e97e5c0f39b7a2b1c3d4e5f6g7
